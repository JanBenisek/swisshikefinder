version: "3.8"

services:
  swisshikefinder:
    image: swisshikefinder:latest
    environment:
      PORT: ${PORT}
      HIKE_API_KEY: ${HIKE_API_KEY}
    restart: always
    ports:
      - "8080:8080"
    # for Air, temp testing
    # volumes:
    #   - ${PWD}:/app