version: "3.8"

services:
  web:
    build: .
    environment:
      PORT: ${PORT}
      HIKE_API_KEY: ${HIKE_API_KEY}
    restart: always
    # extra_hosts:
    #   - "host.docker.internal:host-gateway"
    ports:
      - "8080:8080"